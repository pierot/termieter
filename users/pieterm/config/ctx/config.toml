# ctx (agent history search) config. Symlinked to ~/.ctx/config.toml by install-symlinks.
# Docs: ctx docs show storage

[daemon]
enabled = true

[search]
# Hybrid BM25 + local embeddings (intfloat/multilingual-e5-small). Needs the daemon.
semantic = true

[analytics]
enabled = false
