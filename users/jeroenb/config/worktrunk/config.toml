# Worktree path: hidden folder in the workspace parent, scoped per repo.
# e.g. ~/Work/jackjoe/justified on feat/foo -> ~/Work/jackjoe/.worktrees/justified/feat-foo
worktree-path = "{{ repo_path }}/../.worktrees/{{ repo }}/{{ branch | sanitize }}"
